# T8/T10 搬运清单（2026-08-13 测绘）

> 4 路并行测绘（compose / fastpath / corrector / 接线面）+ 1 路合成。**纯搬运零行为改动**。

## 总览

十批，顺序按「阻碍最少 → 依赖最多」：B1 correct/groupby（纯 AST，零 IO，零 drift 牵连，试点）→ B2 correct 其余五族 → B3 删 Correctors + corrector.rs → B4 compose 纯核 + 52 个测试/fixtures → B5 类型下沉（DirectHit/DirectOutcome/Relation/ExecutionEvidence/ResolvedSlot/IntentSlotKind，fastpath 总闸）→ B6 sales_fact 桥接段 → B7 fastpath 叶子族（7a 底座 / 7b 模板 / 7c 大件）→ B8 agent 侧残留（ODS 推导/库存探针/客户合同/recover_sales_intent）→ B9 三条入口链 + 组合器入口 + 删 wire + direct.rs 整删 → B10 八处口径内联指向 TABLE_SCOPES。

【裁决一处测绘冲突】compose 测绘主张「DirectHit 不下沉、接缝画在 Option<String>」，fastpath/wiring 两份主张「必须下沉」。下沉方胜：docs/ARCHITECTURE.md:179 已把 DirectHit 写死在 semantic lib.rs，且 fastpath 全族返 DirectHit，不下沉则 ARCHITECTURE.md:200 那行永远落不了地。但 compose 测绘的实质贡献成立——compose 纯核（返 Option<String>）对该决定零依赖，所以 B4 排在 B5 之前，先用它趟平新目录与 drift 流程。

【四份测绘全漏的三件事，都会让批次当场红】
1. drift.rs:183 `sql_interpolation_is_allowlisted` 扫 crates/semantic/src **全树且不切 #[cfg(test)]**。direct.rs 有 48 个含 SQL 关键字的 format! 块、38 个不同插值名；corrector.rs 有 6 块 3 个名。每批落地 SQL 构造代码都必须同批往 ALLOW(drift.rs:125-165) 加条目，全程累计约 37 条。其中 corrector.rs:1417 测试里的 `{f}` 连 corrector 测绘也漏了。更重的是 `safe`（relation_rows:1955/1972/1989、stock_product_filtered:2382）来自 rel_quote(direct.rs:1945) 转义后的**用户问句片段**——ALLOW 的门槛写着「必须写明这个值为何不可能来自外部输入」，这两条照抄那句话就是写假证明，理由必须写成「经 rel_quote 单点转义，LIKE 通配符刻意不剥（direct.rs:1942-1944 已知语义放宽）」，且要业主签收。
2. crates/agent/src/ask.rs:2668 `include_str!("../../server/src/direct.rs")`——agent 反向扫 server 源码的镜像漂移锁。direct.rs 一删是**编译错误**不是断言红，必须在 B9 改指 crates/semantic/src/fastpath/finance.rs。
3. B8 的硬阻塞修法测绘给错了方向：direct.rs:3748 那条 `sqlx::query_as` 不能改调 registry::model::load_metrics(model.rs:97)——后者有 `ORDER BY name`、不同 ds 谓词（scoped_pred_1(source_asset_live_pred_at)）、外加 catalog_allows_metric_record 过滤，行集与行序都不同，不是纯搬运。正解是新增 load_metric_sources 逐字带走那条 SQL。

【验收判据本身要修正】tools/eval_cases.json 实际 41 题（37 题带 gold_sql + 4 题 expected_unavailable），「38 题」已过期。且 evaluation.py 的 --progress 行（evaluation.py:470-475 line_of）只记 route 不记 SQL，读不出「逐题结果集字节相同」；它自己在 evaluation.py:16-19 记着 LLM 路径抖动池 ≥9/38≈24%，单趟分辨不出 ±2。所以真正的字节判据是：① `cargo test --workspace`（direct.rs 149 条 + corrector.rs 55 条逐字 SQL 断言，**这是测试必须跟着被测符号同批搬的原因**）；② compose 批用 `why-not-compose --cases tools/eval_cases.json --csv` 前后 CSV 逐字节 diff（确定性、不走 LLM、秒级）；evaluation.py --runs 3 只在里程碑（B3/B4/B6/B7c/B8/B9/B10）跑，且必须同时比对逐趟的「第N趟 通过 X/Y」行——evaluation.py:41-45 明写多趟 exit 0 是比单趟更宽的门。

规模：净删 direct.rs 7363 + corrector.rs 1758 + trait/别名/wire ≈ 9300 行；semantic 新增约 25 个 .rs（34 → 59），lib.rs:15 的「≤38」预算与 ARCHITECTURE.md §4.4 每 crate 预算同批作废重算。

## 门禁改动

- 【订正，不是照办】scripts/check-arch.ps1:68 那句注释「server 是唯一剩下的 WarnOnly：业务代码全部搬出（T10）后删掉 -WarnOnly」**本身是错的**。实测 `crates/server/src` 下 `MySqlPoolOptions|PgPoolOptions|sqlx::query`（去注释行后）命中 58 处，其中 direct.rs 只占 1（ODS 推导 direct.rs:3748）、corrector.rs 占 4（corrector.rs:98/155/362/398）。T8/T10 全做完仍剩 53 处：deep_api 13 / chat 12 / kg_api 10 / query_log 5 / main 3 / trace_api 3 / daily_digest 2 / datamap_api 2 / embed_fill 2 / quality_api 1，全是观测表与 API 自有表。照注释删 `-WarnOnly` 会当场把整条门禁判红，掩盖同一趟里的真违规——脚本自己在 check-arch.ps1:90-98 已经为这个形态写过血书（mcp_api.rs 一句纯注释把门禁染红、盖住了 agent 造连接池的真红）。故 check-arch.ps1:71 的 `-WarnOnly` **保留**，只把第 68 行注释改写成：「剩余 53 处是观测/API 自有表，与 T8/T10 无关；转正条件是 Y2 观测面统一走 kernel::qalog」。
- 【新增 Deny 第 14 条，B9 落地】在 check-arch.ps1 的 ③ 段（第 87 行之后）加：`Deny 'server 不得再向 agent 注入实现' 'crates/server/src' 'HitFn|DetectFn|dms_agent::Correctors|compose_hit|direct_hit|detect_relation'`。这条比「文件不在了」更紧——它挡的是「换个文件名接着注入」。搬完后这六个名字在 crates/server/src 下应一次都不出现（why_not_compose 的调用点在 main.rs:918，但它调的是 dms_semantic::compose::why_not_compose，不在这条模式里）。
- 【新增两条硬缺席断言，B3 与 B9 各兑现一半】`Deny` 函数对不存在的 path 是**判红**（check-arch.ps1:12-21），表达不了「这个文件必须不存在」，而**整文件删除正是本轮唯一的验收物**。在 ⑤ 段之后加裸检查：`foreach ($f in 'crates/server/src/direct.rs','crates/server/src/corrector.rs') { if (Test-Path $f) { Write-Host "[FAIL] $f 仍在（T8/T10 的验收物是整文件删除）" -ForegroundColor Red; $fail = 1 } else { Write-Host "[ok]   $f 已删除" } }`。不走 Deny 故不进 $rules 计数。
- 【新增三条落点存在断言（正面证据）】只证「删了」不够，还要证「搬到位了」，否则「删掉 + 功能丢失」也能全绿：`foreach ($d in 'crates/semantic/src/compose','crates/semantic/src/fastpath','crates/semantic/src/correct') { if (-not (Test-Path $d)) { Write-Host "[FAIL] $d 不存在（T8/T10 的落点，ARCHITECTURE §4.4）" -ForegroundColor Red; $fail = 1 } else { Write-Host "[ok]   $d 已落地" } }`。同样不进 $rules。
- 【$EXPECT_RULES 13 → 14】check-arch.ps1:148。只有上面第 2 条走 Deny 计数；第 3、4 条是裸 Test-Path 不进 $rules。这一行的改动就是 check-arch.ps1:145-147 注释说的「有人动了门禁」的签收，必须与新 Deny 在同一个 commit。
- 【crates/semantic/src/lib.rs:15 的文件数预算作废】那行写着「预算：≤38 个 .rs、单文件 ≤450 行」。今天 semantic 有 34 个 .rs；本轮新增 compose 约 7（mod/assemble/path/values/tests_assemble/tests_path/tests_values）+ fastpath 约 11（mod/hit/doc/text/relation/finance/agg_template/breakdown/stock/miniapp/orders/sales_words/sales_card）+ correct 7（mod/schema/value/agg/agg_rewrite/caliber/groupby）≈ 25 → 约 59 个。lib.rs:15 与 docs/ARCHITECTURE.md §4.4 的每 crate 预算（含 ARCHITECTURE.md:52 那段「超预算时的合并顺序固定：semantic 的 fastpath/{agg,breakdown}→template.rs」）必须同批重算——否则下一个人会照那句话去合并刚拆好的文件。
- 【crates/semantic/tests/drift.rs 两处】(a) EXTERNAL(drift.rs:12-21)：B3 删 `"../server/src/corrector.rs"`、B9 删 `"../server/src/direct.rs"`，各自与对应文件的删除**同一次提交**（早删则少扫一个仍存在的真文件，晚删则 sources() 读不到当场 panic）。剩 `"../policy/src/rules.rs"` 一条。(b) ALLOW(drift.rs:125-165)：累计新增约 37 条，**按批加、每条写实理由**——B2 加 2 条（agg_expr / f）、B4 加 11 条、B6 加 4 条、B7 加约 20 条。两处命名陷阱：ALLOW 是 `file.ends_with(f)` 后缀匹配，fastpath 新文件不许叫 sales_fact.rs（与既有 crates/semantic/src/sales_fact.rs 撞、会让已有的 TABLE/ALIAS 两条无差别放宽）、fastpath 的聚合模板文件不许叫 agg.rs（与 correct/agg.rs 撞）。**`("relation.rs","safe")` 与 `("stock.rs","safe")` 两条的理由不能写「无外部输入入口」**——它们来自 rel_quote 转义后的用户问句片段，理由必须写成「经 rel_quote(单点转义 `\` 与 `'`) ；LIKE 通配符刻意不剥＝已知语义放宽，见原 direct.rs:1942-1944」，这是本轮唯一把既有放宽签收成白名单的地方，需业主认下。
- 【docs/ARCHITECTURE.md 需同批改的行】179（DirectHit 落 semantic lib.rs —— B5 兑现，改成已落地）；196（compose/assemble.rs 那行要求 compose_sql_with 拆 Plan+6 方法 —— 加一句「拆是 T8 之后另一笔提交，本轮只搬」，否则下一个人会在搬运批里顺手拆）；200（fastpath 行的「有效订单状态码 6 处内联」改成 8 处、行号引用 `direct.rs:526-784` 全部过期需重指）；201 与 302（`Corrector` trait + `CorrectCtx` + `run_chain` + `default_chain()` 落 semantic 的设想**作废**，理由见 B3 prep 的三条，改写成「链留在 agent/src/run.rs，semantic 只暴露自由函数」）；405-406（T8/T9 两行的落地状态）；52（每 crate 文件预算与合并顺序重算）。
- 【终态签名，用来对账搬完没搬完】AskDeps 从 16 字段减到 12（删 correctors/detect/compose_hit/direct_hit）；router 从 6 参减到 2 参 `fn router<'a>(embed: &'a EmbedClient, sc_samples: usize) -> Vec<Box<dyn Answerer + 'a>>`；HitFn / DetectFn / graph::Detect / Correctors / Fix 五个别名与 trait 全删；LlmDeps 剩 {embed, sc_samples}、LlmAnswerer::borrowed(embed, sc_samples)。semantic 只暴露纯函数：compose::{try_compose(pg,ds,q), try_compose_metric_only(pg,ds,q,bool), why_not_compose(pg,ds,q)}、fastpath::{try_direct_for(q,warehouse), agg_template, device_orders, balance_ranking, mini_program_order_agg, warehouse_finance, warehouse_sales_question, stock_snapshot, relation::detect_relation}、correct::{fix_select_fields, dedup_select_fields, fix_group_by, fix_time_lower_bound, agg::correct_agg(pg,ds,q,sql), caliber::correct_caliber, value::correct_value, schema::schema_check(pg,ds,sql)}——签名与今天 server 侧实现逐字相同，一个参数都不加。agent 侧留两个同名具名 fn compose_hit / direct_hit（`for<'a> fn(&'a AskCtx<'a>) -> BoxFut<'a, Option<DirectHit>>`）住 crates/agent/src/answerers/hits.rs，router 直接 Box::new 它们。

## B1 correct/groupby —— 试点批（零阻碍、零 drift 牵连）

- 规模：生产 ~270 行（corrector.rs:642-911）+ 测试 ~120 行 → groupby.rs ≈405 行、correct/mod.rs ≈50 行。corrector.rs 7363→ 约 -390 行。；风险：低。唯一坑：expr_has_agg(corrector.rs:894) 的六函数名单（含 group_concat，corrector.rs:899）与 collect_agg_rules(corrector.rs:955) 的五函数名单（corrector.rs:962）**刻意不同**，corrector.rs:897-898 有记账。本批只搬前者，B2 才搬后者，两批之间不许「顺手合并成一份常量」。
- 准备动作：无。这是全仓唯一一处「零准备动作」的搬运：四件全是纯 sqlparser AST 操作、零 IO、零 pg、零 DirectHit、零 AskCtx，且**不含任何 format! SQL 块**（实测 corrector.rs 的 6 个 SQL format! 块全在 schema/value/agg 三族），所以 drift.rs 的 ALLOW 一条都不用加。正因如此它当试点——先把新目录骨架、转发壳形态、对拍流程跑通一遍，再去碰有 drift 牵连的批次。
- 接线：crates/semantic/src/lib.rs 加 `pub mod correct;`（今天 lib.rs:16-30 是 15 条 pub mod）。crates/server/src/corrector.rs 的四个函数体删除改 `use dms_semantic::correct::groupby::{...}`；DmsCorrectors(corrector.rs:19-71) 的对应方法体保持原样纯转发，trait 与注入面本批一行不动。
- 验收：cargo build --workspace && cargo test -p dms-semantic && cargo test -p dms-ai-server && pwsh scripts/check-arch.ps1。本批不改任何 SQL 文本路径，evaluation.py 可不跑；但作为试点建议 `python tools/evaluation.py --runs 1 --filter A` 冒烟一次，确认对拍管道本身可用（--filter 打错会 exit 2，见 evaluation.py:917-919 的反空转闸）。
- 搬运清单：
  - fix_group_by corrector.rs:642 → crates/semantic/src/correct/groupby.rs
  - expr_key / is_expr_item corrector.rs:693 → groupby.rs（pub(crate)）
  - top_select corrector.rs:707 → groupby.rs（pub(crate)，四个校正器共用的顶层单 Select 门控）
  - fix_select_fields corrector.rs:731 → groupby.rs
  - dedup_select_fields corrector.rs:775 → groupby.rs
  - fix_time_lower_bound corrector.rs:808 → groupby.rs（**并入不单独建 time.rs**：单独只 ~135 行，低于 D2 下限 150；并入后 groupby.rs ≈405 行，仍 <450）
  - expr_has_agg corrector.rs:894 → groupby.rs（pub(crate)）
  - 对应测试 corrector.rs:1238-1337 与 1346-1364 → groupby.rs 的 #[cfg(test)] mod tests
  - 新建 crates/semantic/src/correct/mod.rs：pub mod + re-export + collect 包装（corrector.rs:73-76），照 crates/semantic/src/recall/mod.rs 的 54 行形制

## B2 correct 其余五族（agg / agg_rewrite / value / caliber / schema）

- 规模：生产 ~640 行（corrector.rs:78-640 + 913-1167）→ schema 145 / value 270 / agg 165 / agg_rewrite 200 / caliber 215；测试 ~430 行。corrector.rs 退化成约 80 行转发壳。；风险：中。① 两处 OPT_OUT（correct_agg 的 corrector.rs:392-393 与 correct_caliber 的 corrector.rs:579）是刻意不合并的同构词表（corrector.rs:390-391 记账），不许趁机合并；② correct_agg 查 meta.metric 的 `ORDER BY name` 不许丢（顺序即行为）；③ base_table(corrector.rs:628) 的注释 631-634 明说不能换成 strip_annotations，而 direct.rs:1047 用的是 strip_annotations+first_ident_of 的另一种写法，两者行为不同，不许趁机统一；④ 两条 include_str! 源码判据的文件名（corrector.rs → schema.rs / value.rs）与 `\n///` 切段锚点必须同批改，一次红一次绿，不许跨批悬空。
- 准备动作：① **drift.rs ALLOW(crates/semantic/tests/drift.rs:125-165) 加两条**：`("agg.rs","agg_expr")`——parse_agg_rules(corrector.rs:940) 的 `format!("SELECT {agg_expr}")`，值来自 meta.metric 注册表的 agg_expr 列，无外部输入入口；`("agg_rewrite.rs","f")`——corrector.rs:1417 测试里的 `format!("SELECT {f}(o.total_amount) AS \`x\` FROM t_sales_order o")`，f 是本测试字面量数组的元素。**第二条四份测绘全漏**，因为 drift.rs 的 sources() 读整文件、不切 #[cfg(test)]，测试里的 SQL format! 一样判红。② schema_check(126 行)、post_visit_expr(69 行)、rewrite_agg(76 行)、add_scope_filter(56 行) 四处违 D1——**本批只搬不拆**，提取另起一笔。若业主坚持同批拆，提取件一律排在被扫函数**之后**：corrector.rs:1181-1194 的 body() 闭包按 `pub async fn schema_check` 到下一个 `\n///` 切段，提取件插在中间会把段切没。
- 接线：crates/server/src/main.rs:784 的 CLI check-sql 调用点 `corrector::schema_check(owned.pool(), ds_reg::DMS_DS_ID, &args[2])` → `dms_semantic::correct::schema::schema_check(...)`。corrector.rs:1-17 的三条 `pub use`（collect_where_cols / first_ident_of / split_top_and）降级成各新文件自己的普通 use——已确认 direct.rs:285/307/587/764/1047/6455 都直接走 dms_kernel::sql::lex::*，不经这条再导出路径。correct_caliber 里两个 `dms_semantic::` 前缀改 `crate::`（recall/registry 都在同 crate 且不反向依赖 correct，无循环）。
- 验收：cargo build --workspace && cargo test -p dms-semantic && cargo test -p dms-ai-server && pwsh scripts/check-arch.ps1 && cargo run -p dms-ai-server -- check-sql "SELECT 不存在列 FROM t_sales_order"（CLI 调用点改指后的冒烟）。
- 搬运清单：
  - AggRule corrector.rs:913、parse_agg_rule corrector.rs:916(#[cfg(test)])、parse_agg_rules corrector.rs:929、collect_agg_rules corrector.rs:955、last_ident corrector.rs:994(pub(crate))、correct_agg corrector.rs:372 → crates/semantic/src/correct/agg.rs（+测试 corrector.rs:1202-1222, 1365-1378）
  - normalize_agg corrector.rs:1003、proj_has_func_over corrector.rs:1053、rewrite_agg corrector.rs:1091 → crates/semantic/src/correct/agg_rewrite.rs（+测试 corrector.rs:1224-1236, 1379-1469, 1738-1757）
  - ValueMaps corrector.rs:208、Linker corrector.rs:211、impl VisitorMut post_visit_expr corrector.rs:248、link_values_with corrector.rs:322、correct_value corrector.rs:345 → crates/semantic/src/correct/value.rs（+测试 corrector.rs:1470-1589 与源码扫描判据后半 corrector.rs:1196-1199）
  - locate_target corrector.rs:438、has_select_token corrector.rs:490、add_scope_filter corrector.rs:498、correct_caliber corrector.rs:567、base_table corrector.rs:628 → crates/semantic/src/correct/caliber.rs（+测试 corrector.rs:1338-1345, 1590-1737）
  - TABLE_HINT_CAP corrector.rs:78、schema_check corrector.rs:81 → crates/semantic/src/correct/schema.rs（+源码扫描判据前半 corrector.rs:1177-1194）

## B3 删 Correctors trait 与 corrector.rs（第一个里程碑）

- 规模：净删约 1900 行（corrector.rs 1758 + trait/字段/NoFix 桩 ~140），新增 0。；风险：中高。① correct_chain 七步顺序即行为（D9），select-fields → dedup → groupby → agg → caliber → value → time-lower-bound 一步不许换位；② main.rs 两块 deps 只改一块 → HTTP 路径与 ask_prepared 路径（deep/xcx/mcp 走它）Router 组表不同，评测当场分叉；③ drift.rs:18 的删除时机。
- 准备动作：**校正链留在 agent/src/run.rs 原地，semantic 侧不造 Corrector trait / CorrectCtx / run_chain / default_chain**。这与 docs/ARCHITECTURE.md:302 和 §4.4 的 correct/mod.rs 行（写着 trait+CorrectCtx+run_chain+default_chain 四件）**直接冲突，本批必须同时改这两行文档**。三条硬理由：(a) run_chain 需要 cx.pg/cx.question/cx.trace_id，那是 AskCtx，semantic 不许依赖 agent；(b) 链一旦搬出 run.rs，run.rs:390 的 corrector_errors_are_never_silent（切 correct_chain 段数 Err 分支，要求 errs>=3 且 errs==warns）与 run.rs:1685 的 correction_kinds_all_present（14 个 kind 字面量各须在 run.rs 出现 ≥2 次）当场红——链序与留痕的变更原因本就属于 run.rs；(c) 只有一个实现的 trait 在 semantic 侧重建，等于把刚删掉的抽象换个 crate 摆（D7）。另：drift.rs:18 的删除时机是刚性的——早删则少扫一个仍存在的真文件，晚删则 sources() 读不到当场 panic，**必须与 corrector.rs 删除同一次提交**。
- 接线：crates/agent/src/run.rs:57-60 pub type Fix 删；run.rs:62-79 Correctors trait 删（D7：唯一实现是 DmsCorrectors）；run.rs:81-88 LlmDeps.correctors 字段删 → 只剩 {embed, sc_samples}；run.rs:1154 schema_fix 内 `d.correctors.schema_check(...)` 改直调 dms_semantic::correct::schema::schema_check（**保留 `d` 形参，repair 还要它**）；run.rs:1177-1232 correct_chain 的 8 行 `c.xxx(...)` 改直调 dms_semantic::correct::{groupby,agg,caliber,value}::*，log/clip/Err 分支一行不动。crates/agent/src/ask.rs:93 AskDeps.correctors 字段删；ask.rs:266-267 router 调用去 correctors 参；ask.rs:1510-1517 router 签名去 `correctors: &'a dyn Correctors`；ask.rs:1529 改 `LlmAnswerer::borrowed(embed.clone(), sc_samples)`；ask.rs:1791-1818 NoFix 测试桩整块删（28 行）。crates/agent/src/lib.rs:67 `pub use run::{... Correctors, Fix ...}` 删两名。crates/server/src/main.rs:2747 与 2805 两行 `let correctors = corrector::DmsCorrectors;` 删、2756 与 2816 两行 `correctors: &correctors,` 删（**两块必须同批**：只改一块两条入口会组出不同 Router）。main.rs:10 `mod corrector;` 删，corrector.rs 整文件删。crates/semantic/tests/drift.rs:18 的 EXTERNAL 条目 `"../server/src/corrector.rs"` 删。
- 验收：cargo build --workspace && cargo test --workspace（重点看 run.rs:390 与 run.rs:1685 两条源码判据、ask.rs:1894-1907 的 router 契约、main.rs:3513 的 trace_id_reaches_all_three_log_tables——它拿 `let deps = dms_agent::AskDeps {` 当锚点只查 trace_id/conv_id，删字段不破锚，但**必须实跑不许推断**）&& pwsh scripts/check-arch.ps1（server 的 sqlx WarnOnly 计数应从 58 → 54）&& `python tools/evaluation.py --runs 3`（第一个必跑里程碑：校正链调用形态变了）。
- 搬运清单：
  - 无符号移动。纯删除 + 调用点改写。

## B4 compose 纯核 + 36 个单测 + 16 个 fixtures → crates/semantic/src/compose/

- 规模：生产 ~760 行 → mod.rs ~200 / path.rs ~130 / assemble.rs ~400 / values.rs ~150；测试 ~550 行 → 按 assemble/path/values 切三个 tests_*.rs（单文件 220 上下，合规 D2）。direct.rs 7363 → 约 -1300 行。；风险：中高。D9 三处重灾区，逐字复制不许重排不许合并：① pick_inner(direct.rs:397) 的 `max_by_key((词长, 名字))` 元组——tie-break 用 name 定序是「PG 行序不稳」的防线，不许改 key；② value_filters(direct.rs:1117) 的 filter 链顺序——歧义判据必须打在**未经任何其它过滤**的 hits 集合上，不许重排不许合并 filter；③ has_entity_residue(direct.rs:1200) 里硬塞的 `words.push("最低")` 是既有行为，照抄。另：VALUE_APPOSITIVES(direct.rs:1177) 不许顺手并进 dms_kernel::nl::lexicon::STRIP_WORDS——文档注释写着那正是 E16 翻车形态。pick_excluding(direct.rs:386) 那段 40 行文档注释记着 AS1 实证错答，必须整块跟着搬（删了下一个人会把减词判据当多余保险撤掉）。
- 准备动作：① **drift.rs ALLOW 加 11 条**（本批最大隐藏成本，四份测绘无一提及）：`("path.rs","to")`、`("path.rs","on_cond")`、`("path.rs","alias")`——left_join(direct.rs:507) 的 JOIN 骨架；`("assemble.rs","table")`、`("assemble.rs","keys")`、`("assemble.rs","time_and")`、`("assemble.rs","order")`、`("assemble.rs","scope")`、`("assemble.rs","m_src")`、`("assemble.rs","base_alias")`、`("assemble.rs","inner_where")`——全部来自 registry::model 的行类型字段（MetricDef.source_table / JoinEdge / TableSnapshot 的分区键与排序 / TableScope），无外部输入入口，理由写得实。用这条命令机械复核每个新文件：把 drift.rs:190-215 的块扫描逻辑对新文件跑一遍，报什么名就加什么名。② rank_direction/ranking_limit 的使用方**全在 fastpath**（stock_snapshot 的 direct.rs:2476/2477 与 2520/2521），本批落 compose/assemble.rs，B7c 的 stock.rs 从这里 use，**不许再复制一份**。③ **本批不许拆 compose_sql_with_snap**：350 行远超 D1，ARCHITECTURE.md:196 要求拆成 Plan + 六方法，但拆是另一笔提交——同一笔里既搬又拆，出字节差异时分不清是搬坏还是拆坏。同理 timebridge.rs 今天只能装 bind_time_dimension 那 13 行，低于 D2 下限，**别为凑文件名先建空壳**，等 Plan 拆分把时间窗段提出来再建。
- 接线：crates/server/src/direct.rs 删除这些定义、改 `use dms_semantic::compose::{...}`；符号在 semantic 侧升 pub。crates/semantic/src/lib.rs 加 `pub mod compose;`。**5 个混编测试（direct.rs:5009-5029 / 5039-5051 / 5909-5921 / 6490-6515 / 7231-7237）留在 direct.rs**，它们同时断言 compose 与 fastpath，等 B7c 之后再迁；其中 5009/5039/6490 三条是让路门的承重断言（证明门是承重的、不是多余保险），**不许为了搬得动而删或降级**。fixtures 里的 scope_rules/seed_scopes/caliber_of（direct.rs:6450-6488）被混编测试 6490 用，本批把它们提到 semantic 后 server 侧从 semantic use 过去，**不许复制两份**。
- 验收：cargo build --workspace && cargo test -p dms-semantic（36 个搬过去的单测就地自证，这是本批的主判据）&& cargo test -p dms-ai-server（5 个混编测试仍绿）&& pwsh scripts/check-arch.ps1 && **`cargo run -p dms-ai-server -- why-not-compose --cases tools/eval_cases.json --csv before.csv` 与 after.csv 逐字节 diff**（main.rs:846 的确定性诊断口，八列 idx/case/question/gate/composable/reason/hardcoded/tags，不走 LLM、秒级，是 compose 批真正的字节判据）&& python tools/evaluation.py --runs 3。
- 搬运清单：
  - compose/mod.rs：compose_gated direct.rs:275、is_time_expr direct.rs:324、pick direct.rs:358、pick_excluding direct.rs:386、pick_inner direct.rs:397、metric_word direct.rs:421(#[cfg(test)])、metric_dimension_allowed direct.rs:425；本地保留 `type DimDef = DimensionDef;` 别名以保逐字搬运
  - compose/path.rs：table_eq direct.rs:435、find_path direct.rs:440、find_edge direct.rs:483、left_join direct.rs:507、caliber_in_on direct.rs:520
  - compose/assemble.rs：rank_direction direct.rs:333、ranking_limit direct.rs:343、sql_has_keyword direct.rs:547、compose_sql_with_snap direct.rs:552（350 行主体）、bind_time_dimension direct.rs:905
  - compose/values.rs：has_residue direct.rs:1095、value_filters direct.rs:1117、VALUE_APPOSITIVES direct.rs:1177、consumed_phrase direct.rs:1179、registry_words direct.rs:1192、has_entity_residue direct.rs:1200
  - compose/tests_{assemble,path,values}.rs：compose_sql direct.rs:498 与 compose_sql_with direct.rs:534 两个 #[cfg(test)] 入口 + 36 个纯 compose 单测（direct.rs:4176-4182 / 4901-4917 / 4930-4963 / 4971-4999 / 5056-5061 / 5558-5615 / 5620-5639 / 5653-5673 / 5690-5752 / 5854-5906 / 5924-6041 / 6081-6128 / 6160-6246 / 6254-6395 / 6716-6727 / 7101-7196）+ 16 个 fixtures（direct.rs:4168-4174 / 5519-5556 / 5641-5648 / 5680-5688 / 5836-5852 / 5990-5996 / 6043-6079 / 6184-6196 / 6450-6488）

## B5 类型下沉（fastpath 总闸，零逻辑改动）

- 规模：移动约 90 行类型定义 + 约 15 处 use/re-export 改写。；风险：中。① Relation 的 `Debug` 输出直接进 AskResult.sql（回归断言形如 `[AGE 图查询] BuyersOfGoods("烤肠")`），三个变体名与派生顺序一字不许改；② DirectHit 八个字段名同理，前端可见；③ 这是全区总闸——不做它，B6/B7 每一批都卡住。
- 准备动作：crates/agent/src/intent.rs 按 D3 切纯数据那半：IntentSlotState(intent.rs:63)、IntentSlotSummary(intent.rs:69)、IntentCoverageSummary、IntentSummary 是 LLM 意图合同，**留 agent**；只下沉 IntentSlotKind + ResolvedSlot + ExecutionEvidence + 它的 impl（resolve / with_detail 等）。已核实这四者只依赖 std + serde::Serialize，semantic 的 Cargo.toml 两样都有，零新增依赖（D6）。另需先复核 crates/agent/src/answerers/hits.rs 的 5 条 include_str 自扫（hits.rs:514/535/563/611/642）有没有锚在 DirectHit 的定义文本上——锚在 land()(hits.rs:108-262) 上的不受影响。
- 接线：agent 侧三处改 re-export 保住既有路径、全仓零 use 改动：hits.rs 改 `pub use dms_semantic::{DirectHit, DirectOutcome};`（保住 crate::answerers::hits::DirectHit）、intent.rs 改 `pub use dms_semantic::{ExecutionEvidence, ResolvedSlot, IntentSlotKind};`、graph.rs 改 `pub use dms_semantic::fastpath::relation::Relation;`。crates/server/src/direct.rs:19-21 那三行 `pub use dms_agent::answerers::{graph::Relation, hits::{DirectHit, DirectOutcome}}` 方向反转成 `dms_semantic::…`——**这三行是 T9 反向依赖边的物证，本批就是拔掉它**。
- 验收：cargo build --workspace && cargo test --workspace && pwsh scripts/check-arch.ps1（⑤ 段依赖方向解析必须仍报 ≥10 条边且单向）。零逻辑改动、零 SQL 文本变化，evaluation.py 可只跑 --runs 1 冒烟。
- 搬运清单：
  - DirectOutcome crates/agent/src/answerers/hits.rs:23-27 → crates/semantic/src/lib.rs
  - DirectHit crates/agent/src/answerers/hits.rs:29-54（8 字段 outcome/sql/route/prev/comparisons/detail/sales_context/intent_evidence）→ crates/semantic/src/lib.rs（docs/ARCHITECTURE.md:179 已写死这个落点）
  - IntentSlotKind crates/agent/src/intent.rs:49-60 → crates/semantic/（新 intent_evidence.rs）
  - ExecutionEvidence crates/agent/src/intent.rs:91-98 + ResolvedSlot crates/agent/src/intent.rs:99-103 + impl ExecutionEvidence crates/agent/src/intent.rs:105 起 → 同上
  - Relation crates/agent/src/answerers/graph.rs:22-34 → crates/semantic/src/fastpath/relation.rs（本批先建 fastpath/mod.rs + relation.rs，只放类型，判定函数 B7b 才来）

## B6 sales_fact 桥接段 direct.rs:1305-1943 → crates/semantic/src/fastpath/

- 规模：639 行（direct.rs:1305-1943）→ 2 文件：sales_words.rs ~250、sales_card.rs ~390（逼近 D2 上限 450，不许再并）。测试另计。；风险：中。这是「不先搬后面全部返工」的那一批：agg_template(direct.rs:3060)、sales_breakdown(direct.rs:2895)、try_direct_for(direct.rs:1273)、customer_name_fragment(direct.rs:3909)、try_compose(direct.rs:214)、try_compose_metric_only(direct.rs:947) 六个调用点全依赖它。它只依赖 dms_semantic::sales_fact + kernel，本就该在 semantic，跨 crate 零阻碍（B5 已把 DirectHit 下沉）。
- 准备动作：① **recover_sales_intent 不在本批**：它返回 dms_agent::intent::IntentAttempt，semantic 引不到 agent。它留在 direct.rs 直到 B8。②**新文件不能叫 sales_fact.rs**：crates/semantic/src/sales_fact.rs 已存在，且 drift.rs 的 ALLOW 已有 `("sales_fact.rs","TABLE")` 与 `("sales_fact.rs","ALIAS")` 两条——ALLOW 的匹配是 `file.ends_with(f)` 后缀匹配，同名新文件会让这两条无差别放宽到新文件上，等于悄悄开一道口子。故命名 sales_card.rs / sales_words.rs。③ drift.rs ALLOW 加 4 条：`("sales_card.rs","advice")`、`("sales_card.rs","names")`、`("sales_card.rs","reason")`、`("sales_card.rs","requested")`——均为「不可计算」卡的中文文案片段，来自本文件常量与已消化词表。
- 接线：direct.rs 改 use。**warehouse_sales_metrics 与 warehouse_sales_fact 必须设成 pub 而非 pub(crate)**：B8 的 recover_sales_intent 进 agent 后要跨 crate 调它们。同批需 pub 的还有 warehouse_sales_question（compose_hit 让路门用）、warehouse_sales_has_unsupported_semantics / warehouse_sales_dimensions / warehouse_sales_fact（sales_breakdown 用）、sales_fact_metric_extra_words（customer_name_fragment 用）。
- 验收：cargo build --workspace && cargo test --workspace（该段测试同批搬，就地自证 SQL 字节）&& pwsh scripts/check-arch.ps1 && python tools/evaluation.py --runs 3。
- 搬运清单：
  - sales_fact_metric_extra_words direct.rs:1305、sales_fact_dimension_extra_words direct.rs:1315、longest_sales_fact_word direct.rs:1325、warehouse_sales_metrics direct.rs:1338、warehouse_sales_dimensions direct.rs:1370、warehouse_order_count_question direct.rs:1414、warehouse_sales_question direct.rs:1422、WAREHOUSE_SALES_UNSUPPORTED direct.rs:1426、warehouse_sales_unsupported_semantic direct.rs:1438、warehouse_sales_has_unsupported_semantics direct.rs:1453、answerable_tail_words direct.rs:1467、sales_fact_consumed direct.rs:1512、unrecognized_residue direct.rs:1544 → crates/semantic/src/fastpath/sales_words.rs
  - sales_fact_unavailable direct.rs:1552、warehouse_sales_semantics_unavailable direct.rs:1578、warehouse_sales_time_bounds direct.rs:1610、sales_fact_sql direct.rs:1614、warehouse_sales_fact direct.rs:1633、intent_time_surface direct.rs:1690、sales_fact_province_filter direct.rs:1717、warehouse_sales_fact_predicated direct.rs:1772 → crates/semantic/src/fastpath/sales_card.rs
  - 该段对应的测试（direct.rs:4053-7363 内按被测符号名筛）同批搬

## B7 fastpath 叶子族（7a 底座 → 7b 模板 → 7c 大件，三步各自可编译）

- 规模：7a ~240 行、7b ~360 行、7c ~560 行生产码；测试合计 ~1500 行。direct.rs 7363 → 约 -2660 行（含测试）。；风险：高，全程 D9 密度最高。逐条：① MARKET_COST_GROUPS(direct.rs:2013) 43 个物理列名的**分组内顺序直接决定 COALESCE 相加的字符串顺序＝SQL 字节**，一个列名都不许排序；② agg_strip_words(direct.rs:3043) 的 AGG_WORDS 长词先于子串（成交客户数→成交客户→客户数），chain 顺序即行为，direct.rs 有 agg_template_time_words_come_from_the_single_source 逐词钉住；③ strip_relation_words(direct.rs:1241) 的单字词（有/的/是/都/买）**只能边界剥**，direct.rs:1250-1252 注释写明全局 replace 会把「美的冰箱」吃成「美冰箱」；④ stock_product_fragment(direct.rs:2194) 的 PREFIXES 词表顺序 + 循环内 strip_prefix 的 if/else if 分支顺序都是行为，direct.rs:2196-2197 注释同样记着「美的冰箱」翻车；⑤ mini_program_order_agg 的列名 `todaty_zyorder_count`/`todaty_zyorder_amount`（direct.rs:2632-2635）是库里的**真实错拼**，任何「修正」都会让 SQL 打不到列；⑥ sales_order_rows 两支口径**不一致**（direct.rs:2800 的客户名单支带 order_status NOT IN，direct.rs:2817 的明细支只有 deleted_flag = 0）——这是既有的、评测已锁定的行为，搬运时**严禁对齐**；⑦ relation_rows 三条 SQL 高度相似但投影列名/GROUP BY/ORDER BY 各不同，字节判据下不许合并去重；⑧ warehouse_order_hit(direct.rs:2984/2991) 的 `o.deleted_flag = 0` 是 JOIN ON 上的软删条件、不是有效订单口径，不在 B10 的 8 处名单里，不许顺手改；⑨ 两张「不可计算」卡的投影头 `'不可计算' AS \`数据状态\`` 被 is_unavailable_card(direct.rs:3241) 按子串识别、进而决定要不要跌进 ODS 推导，文案一个字都不能动。
- 准备动作：① **drift.rs ALLOW 约 20 条**，按新文件名逐条加，实测名单：`("relation.rs","safe")`；`("finance.rs","detail"/"name"/"p"/"pred"/"top_n")`；`("stock.rs","ZT_FROM"/"ZT_WHERE"/"column"/"label"/"safe"/"where_sql")`；`("miniapp.rs","note"/"region_sql"/"select_cols"/"snapshot")`；`("orders.rs","region_sql"/"status_sql"/"time"/"where_sql")`；`("doc.rs","code"/"group"/"joins"/"status"/"where_sql")`；`("agg.rs","metric"/"pred")`（注意 agg.rs 这个名字与 B2 的 correct/agg.rs 后缀相同——ALLOW 是后缀匹配，两文件会互相放宽，**fastpath 侧改名 agg_template.rs**）。② **`safe` 两条的理由不能照抄「无外部输入入口」，那是假的**：它来自 rel_quote(direct.rs:1945) 转义后的用户问句片段（relation_rows 的 direct.rs:1967/1984/2002/2005）。ALLOW 的门槛注释（drift.rs:124-125）要求写明为何不可能来自外部输入——这两条只能写成「经 rel_quote 单点转义（`\` 与 `'`），且 direct.rs:1942-1944 注释写明 LIKE 通配符刻意不剥＝已知语义放宽」。这是本轮唯一把既有放宽**签收成白名单**的地方，需业主认下，不能由搬运工默默添。③ 5 个混编测试（direct.rs:5009/5039/5909/6490/7231）到 7c 结束才迁 compose/tests_*.rs。
- 接线：direct.rs 改 use，各符号升 pub（compose_hit 的六个让路门 agg_template/device_orders/balance_ranking/warehouse_sales_question/warehouse_finance/mini_program_order_agg 与 hardcoded_producer 的四支 sniff_doc_code/device_orders/agg_template/sales_breakdown 都要跨模块可见）。rank_direction / ranking_limit 从 B4 的 crate::compose::assemble use 过来，**不许复制**。stock_product_snapshot(direct.rs:2319) 构造 ExecutionEvidence + IntentSlotKind——B5 已下沉，此处直接用 semantic 本地类型。
- 验收：每一步：cargo build --workspace && cargo test --workspace（各族模板测试就地自证 SQL 逐字节）&& pwsh scripts/check-arch.ps1。7c 结束后：python tools/evaluation.py --runs 3。
- 搬运清单：
  - 7a-text：rel_quote direct.rs:1945、RESIDUAL_PUNCT direct.rs:2913、residual_text direct.rs:2915、customer_name_fragment direct.rs:3909 → crates/semantic/src/fastpath/text.rs
  - 7a-doc：sales_status_sql direct.rs:2934、sales_detail_sql direct.rs:2949、warehouse_order_hit direct.rs:2971、document_detail_sql direct.rs:2998、sniff_doc_code direct.rs:3011 → crates/semantic/src/fastpath/doc.rs
  - 7b-relation：detect_relation direct.rs:1214、strip_relation_words direct.rs:1241、relation_rows direct.rs:1951 → crates/semantic/src/fastpath/relation.rs（B5 已建，此处加判定与 SQL）
  - 7b-finance：MARKET_COST_GROUPS direct.rs:2013、market_cost_expr direct.rs:2026、market_cost_where direct.rs:2030、warehouse_market_cost direct.rs:2036、warehouse_invoice_unavailable direct.rs:2075、warehouse_account_bill_unavailable direct.rs:2085、warehouse_finance direct.rs:2095、balance_ranking direct.rs:2113 → crates/semantic/src/fastpath/finance.rs
  - 7b-agg：agg_strip_words direct.rs:3043、agg_template direct.rs:3059、time_window direct.rs:3132 → crates/semantic/src/fastpath/agg.rs；sales_breakdown direct.rs:2895 → crates/semantic/src/fastpath/breakdown.rs
  - 7c-stock：stock_province_predicate direct.rs:2153、stock_product_fragment direct.rs:2194、stock_sku_predicate direct.rs:2315、stock_product_snapshot direct.rs:2319、stock_product_unavailable direct.rs:2347、stock_snapshot direct.rs:2434 → crates/semantic/src/fastpath/stock.rs（**stock_product_filtered direct.rs:2358 不在本批，见 B8**）
  - 7c-miniapp：province_region_qualifier direct.rs:2560、mini_program_order_agg direct.rs:2586 → crates/semantic/src/fastpath/miniapp.rs
  - 7c-orders：sales_order_rows direct.rs:2742、device_orders direct.rs:2826 → crates/semantic/src/fastpath/orders.rs
  - 各族测试随各自被测符号同批搬（这是唯一的 SQL 字节守卫）

## B8 agent 侧残留（policy / AskCtx / entity_resolver / prompt 四条边挡住的部分）

- 规模：移出 direct.rs 约 790 行（2358-2432 的 75 + 3231-3907 的 677 + 4017-4051 的 35）；agent 新增 3-4 个文件。direct.rs 7363 → 约 -790 行。；风险：高。① semantic 的 Cargo.toml 第 6 行是硬规则「不得依赖 dms-policy」、check-arch.ps1:85-87 是它的门禁——库存这一支被**劈成两半**（纯拼串在 semantic/fastpath/stock.rs、带闸门取数的探针在 agent），是本轮第二大风险点；② customer_filtered_sales 的歧义/零命中/故障三分支回落语义一字不许改；③ 本批只搬产出方，direct_hit(direct.rs:3183) 的 wire 本体留到 B9——它的四臂 match 顺序与 `or(Some(hit))` 形态被 direct.rs:6830 与 direct.rs:4493 两处源码扫描逐字钉死。
- 准备动作：**本区唯一硬阻塞，必须先做**：direct.rs:3748 那条 `sqlx::query_as("SELECT name, source_table FROM meta.metric WHERE ds_id IN ($1, '*') AND status='active'")` 一进 agent 就撞 scripts/check-arch.ps1:55-56 的 FAIL 级红线（agent 不得 sqlx::query*，非 WarnOnly）。**修法不是改调 dms_semantic::registry::model::load_metrics**——已核实 model.rs:97-107 那条 SQL 带 `ORDER BY name`、用 `scoped_pred_1(source_asset_live_pred_at)` 而非裸 `ds_id IN ($1,'*')`、外加 catalog_allows_metric_record 过滤，**行集与行序都不同**，换过去不是纯搬运，而这份清单是闸 1 通道②的词匹配语料，行序影响命中。正解：在 crates/semantic/src/registry/model.rs 新增 `pub async fn load_metric_sources(pg: &PgPool, ds: &str) -> Vec<(String, String)>`，**逐字带走 direct.rs:3748-3760 的 SQL 文本与 warn→空表降级形态**。已核对：该 SQL 含内联 `ds_id IN ($1, '*')` 且窗口内无 `JOIN meta.`，drift.rs:82-84 的 `!joined && win.contains("ds_id")` 分支放行；它是 `&str` 字面量不是 format!，故 ALLOW 零新增。另：stock_probe 与 customer_sales 的纯函数助手（stock_product_fragment / stock_sku_predicate / stock_product_snapshot / stock_product_unavailable / customer_name_fragment / warehouse_sales_fact_predicated）已在 B6/B7 落 semantic，本批只搬带闸门/带主档的探针本体——**这条切法要在动手前认下，否则库存这一支会被来回搬两次**。
- 接线：crates/server/src/main.rs:2326 的 `direct::recover_sales_intent(&question.effective_question, st.mysql.is_warehouse())` → `dms_agent::…::recover_sales_intent`。direct.rs:7057 的 `include_str!("../../semantic/src/recall/ods.rs")`（derive_gate2_evidence_fetch_is_one_scoped_query）随 ODS 推导测试进 agent，路径变 `../../../semantic/src/recall/ods.rs`。
- 验收：cargo build --workspace && cargo test --workspace && pwsh scripts/check-arch.ps1（**重点看 `agent 不得造连接池 / 不得 sqlx::query*` 必须仍是 [ok] 而非 [FAIL]**）&& python tools/evaluation.py --runs 3。
- 搬运清单：
  - stock_product_filtered direct.rs:2358（吃 dms_policy::{Principal,Scope} + dms_connector::SqlSource + gate_on + EXEC_TIMEOUT）→ crates/agent/src/answerers/stock_probe.rs
  - customer_filtered_sales direct.rs:4017（吃 &AskCtx + dms_agent::entity_resolver::{resolve_customer, CustomerResolution, CustomerMatchField}）→ crates/agent/src/answerers/customer_sales.rs
  - ODS 推导整块 direct.rs:3231-3907（DERIVE_ROUTE:3231、DERIVE_TOP_K:3234、DERIVE_TEMP:3237、is_unavailable_card:3241、derive_eligible:3247、derive_tables_allowed:3255、analyze_derive_sql:3299、analyze_query:3312、analyze_set_expr:3325、ident_norm:3340、TIME_ALIAS_WORDS:3345、TIME_FNS:3349、is_time_bucket_alias:3358、expr_calls_time_fn:3363、collect_from_factor:3406、analyze_select:3440、is_literal_projection:3512、expr_qualifier_refs:3517、join_on_expr:3539、collect_eq_pairs:3556、CORE_SALES_METRIC_WORDS:3591、is_measure_col:3598、derive_labels_ungrounded:3608、derive_joins_unevidenced:3652、bare_table:3685、derive_compose:3691、DeriveTry:3728、ods_derive:3734、derive_pool_winc_guard:3811、derive_attempt:3824）→ crates/agent/src/answerers/derive.rs + derive_shape.rs（677 行，D2 必须切 2-3 文件）
  - recover_sales_intent（direct.rs:1305-1943 段内，返回 dms_agent::intent::IntentAttempt）→ crates/agent/src/answerers/derive.rs 或 crates/agent/src/intent.rs

## B9 三条入口链 + 组合器入口 + 删 wire + direct.rs 整删（终局里程碑）

- 规模：移出 direct.rs 约 330 行（57-258 的 202 + 942-1093 的 152 中未搬部分 + 1263-1303 的 41 + 3143-3229 的 87）；净删 direct.rs 剩余全部 + AskDeps/router/五个别名约 60 行。；风险：最高——这是唯一一批「删了就回不去」的。try_direct(direct.rs:1263) 的 7 支与 try_direct_warehouse(direct.rs:1287) 的 11 支 or_else **顺序即行为**，direct.rs:1294-1296 有专门注释解释 mini_program_order_agg 必须排在 sales_order_rows 之前（否则静默丢「小程序」限定）；compose_verdict(direct.rs:107，82 行超 D1) 与 try_compose 共用同一批判据，**只许搬不许拆**（诊断必须与判定同源，direct.rs:84-87 明写这条纪律：分开搬会漂成「诊断说能装配、实际回落 LLM」）；metric_only(direct.rs:982，105 行) 同理，且它构造伪维度 `DimDef{name:"",expr:""}` 触发装配器的无维度模式，字段逐字相同。
- 准备动作：① **`macro_rules! reg_load`(direct.rs:200) 靠 textual scope，落 compose/mod.rs 时必须排在 try_compose 之前**——这是全区唯一一处「顺序即编译」。② **compose_hit:3161 的让路门必须是 agg_template 而不是 try_direct**（direct.rs:3155-3160 注释：写成 try_direct 会让所有销售额×维度问句让路给硬编码模板，作者自述第一版就写错过）——转写时最容易写错的一行；好在 direct.rs:4526 与 4619 两条源码扫描正是钉这个的，测试跟着搬就能当场抓住。③ **crates/agent/src/ask.rs:2575 的 `DimensionFirstHit::new(direct_hit)` 逐字断言**：新的具名 fn 必须仍叫 `direct_hit`。④ **crates/agent/src/ask.rs:2668 的 `include_str!("../../server/src/direct.rs")` 必须改指 crates/semantic/src/fastpath/finance.rs**（四份测绘全漏）——它是 unavailable_card_mark_mirrors_direct_rs 的镜像漂移锁，扫 `'不可计算' AS \`数据状态\``，宿主是 B7b 落地的 warehouse_invoice_unavailable/warehouse_account_bill_unavailable。direct.rs 一删这里是**编译错误**不是断言红。⑤ direct.rs 的 4 处自扫（direct.rs:4493 direct_hit 四臂、4526 balance_ranking 让路门、4619 mini_program 让路门、6830 推导接线）随各自被测符号迁到新宿主并改 include_str! 路径；**direct.rs:4872 那条否定式全文件断言（`direct.rs 不得复制事实合同`，禁 DWS_OFFLINE_SALE/DwsMetric/DwsDim 三个字面量）在文件切成 15 个后失去宿主**——要么按新文件逐个重挂，要么在 ARCHITECTURE 里明确记账删除，不许默默丢。
- 接线：crates/agent/src/ask.rs:44-52 HitFn 删、:53 DetectFn 删、:94-96 AskDeps 三字段 detect/compose_hit/direct_hit 删（16 字段 → 12，B3 已删 correctors）；ask.rs:1510-1517 router 改 `fn router<'a>(embed: &'a EmbedClient, sc_samples: usize) -> Vec<Box<dyn Answerer + 'a>>`；ask.rs:266-267 调用点同步；ask.rs:1520 `GraphAnswerer::new(Box::new(detect))` 改零参、graph.rs:38 `pub type Detect` 删、graph.rs:45 new 去参（graph.rs:50 的 hit() 保留供单测）。crates/agent/src/lib.rs:46-48 删 DetectFn/HitFn 两名 + lib.rs:44-45 那两行 T8/T10 临时入参注释。crates/server/src/main.rs:2760-2763 与 2817-2819 两块 deps 删（**必须同批**）；main.rs:918 `direct::why_not_compose` → `dms_semantic::compose::why_not_compose`；main.rs:13 `mod direct;` 删；direct.rs 整文件删。crates/semantic/tests/drift.rs:18 EXTERNAL 删 `"../server/src/direct.rs"`（与文件删除同一次提交）。
- 验收：cargo build --workspace && cargo test --workspace && pwsh scripts/check-arch.ps1（含本批新增的门禁规则，见 gate_changes）&& `cargo run -p dms-ai-server -- why-not-compose --cases tools/eval_cases.json --csv final.csv` 与 B4 的 after.csv 逐字节 diff && **python tools/evaluation.py --runs 3**（终局判据；比对逐趟「第N趟 通过 X/Y」行 + 失败集交集为空）。
- 搬运清单：
  - crates/semantic/src/compose/mod.rs 收尾：hardcoded_producer direct.rs:57、doc_binding_hit direct.rs:74、why_not_compose direct.rs:88、compose_verdict direct.rs:107、`macro_rules! reg_load` direct.rs:200、try_compose direct.rs:212、try_compose_metric_only direct.rs:942、enum MetricOnly direct.rs:965、metric_only direct.rs:982
  - crates/semantic/src/fastpath/mod.rs：try_direct direct.rs:1263（7 支 or_else）、try_direct_for direct.rs:1273、try_direct_warehouse direct.rs:1287（11 支 or_else）
  - crates/semantic/src/fastpath/hit.rs：hit direct.rs:32（DirectHit 的唯一构造点，约 30 个调用点，设 pub(crate)）
  - crates/agent/src/answerers/hits.rs：具名 fn `compose_hit`（转写 direct.rs:3143-3181）与 `direct_hit`（转写 direct.rs:3183-3229）——两者吃 AskCtx，只能留 agent
  - 5 个混编测试 direct.rs:5009-5029 / 5039-5051 / 5909-5921 / 6490-6515 / 7231-7237 → crates/semantic/src/compose/tests_*.rs

## B10 八处有效订单口径内联 → dms_semantic::seed::TABLE_SCOPES

- 规模：8 行改动 + 8 处 use（约 20 行净变化）。；风险：低-中。⑥⑦⑧ 三处在 T8/T10 迁移路径之外（entity.rs 是实体卡、daily_digest.rs 是日报），属另一个变更原因（D3），所以单独一批而不是塞进 B7 是对的。注意别把 warehouse_order_hit(direct.rs:2984/2991) 的 `o.deleted_flag = 0` 算进来——那是 JOIN ON 上的软删条件不是有效订单口径；也别把 balance_ranking 的 `balance_status='4' AND balance_type IN ('8','9')`（direct.rs:2141）和 stock_snapshot 的 `inventory_status = 'ZP'`（direct.rs:2461）算进来——它们同属「口径内联」但是另外的口径，seed.rs 有对应 table_scope 行，可同一手法处理但应单独立项。
- 准备动作：① **只指常量、不读 DB**。单一事实源已存在且值逐字相同：crates/semantic/src/seed.rs:416-419 的 `pub const TABLE_SCOPES` 第一行就是 `("t_sales_order", "deleted_flag = 0 AND order_status NOT IN ('0','108','199')", …)`；`o.` 限定形态由 `dms_kernel::sql::lex::qualify_cols(filter, "o")` 产出，direct.rs:5857-5858 的既有断言已逐字证明它等于 `o.deleted_flag = 0 AND o.order_status NOT IN ('0','108','199')`。故 8 处可做到零字节差。② **不要在同一批改成运行时读 meta.table_scope**：⑤⑧ 在同步无 pg 的调用链上（try_direct 是 `fn`、ORDERS_SQL 是 `const`），要读库就得把 6 个函数改 async 并把 pg/ds 一路穿参——那是签名与失败语义的改动；且库里那行若与 seed 不一致或读失败，评测立刻出字节差。运行时读 table_scope 应作为独立提交，且必须 fail-closed 回落到同一个常量。③ **搬运前先改 docs/ARCHITECTURE.md:200**：那行写的「有效订单状态码 6 处内联」和行号引用 `direct.rs:526-784` **两者都已过期**（direct.rs 内实际 5 处、跨仓共 8 处），按错的数收工会漏掉三处。
- 接线：① relation_rows / BuyersOfGoods（原 direct.rs:1966，`o.` 限定）→ 现 crates/semantic/src/fastpath/relation.rs；② relation_rows / GoodsOfCustomer（原 direct.rs:1983）→ 同上；③ relation_rows / Copurchase（原 direct.rs:2004）→ 同上；④ sales_order_rows / wants_customers 支（原 direct.rs:2800）→ 现 crates/semantic/src/fastpath/orders.rs；⑤ agg_template 的 base 闭包（原 direct.rs:3119，裸列）→ 现 crates/semantic/src/fastpath/agg_template.rs；⑥ crates/agent/src/answerers/entity.rs:1203（customer_card 的 orders_sql，`o.` 限定）；⑦ crates/agent/src/answerers/entity.rs:1386（goods_card 的 stats_sql，`o.` 限定）；⑧ crates/server/src/daily_digest.rs:66（ORDERS_SQL 常量，裸列）。
- 验收：cargo build --workspace && cargo test --workspace（qualify_cols 那条既有断言 + 各模板的逐字 SQL 断言 + entity.rs / daily_digest.rs 自己的断言）&& pwsh scripts/check-arch.ps1 && python tools/evaluation.py --runs 3。
- 搬运清单：
  - 无符号移动。只把 8 处字面量 `deleted_flag = 0 AND order_status NOT IN ('0','108','199')` 的来源改成单一事实源。